# 根据 AppID 自动获取并填充游戏信息

日期：2026-09-03  
状态：第一版已在工作区实现，待发布；完整多 Depot 支持仍为后续扩展

## 1. 需求目标

在「自定义 Manifest」页面增加「自动获取游戏信息」按钮。用户填写 AppID 后，点击一次即可获取并填充 DepotID、显示名称、安装目录名、Steam 占位文件名，以及 BuildID 和 Manifest，减少手动查询和填写错误。

对于数据完整、Depot 和 Windows 启动 EXE 均能唯一确定的游戏，应一次完成全部填充。存在多个候选时，应展示候选供用户选择，不应随意采用第一个结果。

本需求总结基于当前讨论；多 Depot 的完整支持属于后续扩展建议，不代表已确定纳入第一版。

## 2. 数据来源与字段对应

复用项目现有的第三方 SteamCMD API：

```text
GET https://api.steamcmd.net/v1/info/{AppID}
```

以下路径以响应中的 `data[AppID]` 为起点：

| 页面字段 | 数据来源 | 填充规则 |
| --- | --- | --- |
| 显示名称 | `common.name_localized`、`common.name` | 建议优先采用界面语言对应的本地化名称，缺失时采用通用名称 |
| 安装目录名 | `config.installdir` | 保留登记值，不从显示名称推算 |
| Steam 占位文件名 | `config.launch.*.executable` | 筛选 Windows 启动项后，按完整相对路径去重并确定候选 |
| DepotID | `depots` 下的数字键 | 结合平台、架构、语言等可用信息筛选，不采用 AppID + 1 等推算规则 |
| BuildID | `depots.branches.public.buildid` | 第一版默认使用公开分支 `public` |
| Manifest | `depots[DepotID].manifests.public` | 读取字符串值，或对象中的 `gid`；必须与所选 Depot 对应 |

同一次获取应复用一份响应，避免为每个字段重复请求。接口无需登录，但属于第三方服务，可能超时、不可用或缺少部分字段。

SteamDB 配置页作为手动查询入口：`https://steamdb.info/app/{AppID}/config/`；Depot 查询入口为 `https://steamdb.info/app/{AppID}/depots/`。不将抓取 SteamDB 页面作为自动填充方案。

## 3. 已验证示例

2026-09-03 实际查询 AppID `4162040` 的接口响应：

| 字段 | 查询结果 |
| --- | --- |
| AppID | `4162040` |
| DepotID | `4162041` |
| 中文显示名称 | `绝区零` |
| 通用显示名称 | `Zenless Zone Zero` |
| 安装目录名 | `Zenless Zone Zero` |
| Steam 占位文件名 | `HYP.exe` |
| public BuildID | `24944365` |
| public Manifest | `5086026690313915172` |

该示例只有一个 Depot 和一个 Windows 启动项，可以实现一键填满。以上为查询时的结果，尤其 BuildID 和 Manifest 会随游戏更新变化，不应硬编码。

## 4. Depot 与启动 EXE 的关系

Depot 是游戏内容的分组，可能分别存放公共资源、平台文件、语言包或 DLC。启动 EXE 来自 Steam 登记的启动选项。两者没有一一对应关系：

- 多个 Depot 可以共用一个启动 EXE。
- 一个 Depot 可以包含多个启动 EXE。
- 多个启动选项可以指向同一个 EXE，只是启动参数不同。
- 不能根据 DepotID 直接确定启动 EXE，也不能将全部 Depot 视为互相替代的选项。

### 4.1 Depot 处理规则

1. 从 Depot 元数据中提取候选，排除 `branches` 等非 Depot 字段。
2. 根据可获得的平台、架构、页面所选语言和 DLC 等信息筛选。未声明限制不等于不适用。
3. 对能够明确确定的单 Depot 场景，自动填入 DepotID 及对应的 public Manifest。
4. 对多个候选，展示 DepotID、名称（若有）和可用的平台／语言信息，允许用户选择，并同步对应 Manifest。
5. 若多个 Depot 实际需要共同使用，第一版应明确提示当前单 Depot 限制，不能声称选择一个即可生成完整安装配置。
6. 缺少足够元数据时保留不确定状态；候选筛选不能保证识别出所有游戏的安装需求。

### 4.2 启动 EXE 处理规则

1. 排除明确仅适用于 Linux、macOS 等其他平台的启动项；未声明平台时结合 EXE 类型作为候选判断依据。
2. 提取完整的可执行文件相对路径，保留子目录，例如 `Bin\\Launcher.exe`。
3. 按 Windows 路径语义规范化分隔符并去重；用于填入的路径仍需保留正确目录结构。
4. 唯一 EXE 路径直接填入；多个路径时展示启动项名称、路径及必要的参数信息，让用户选择。
5. 同一 EXE 配置不同启动参数时，对占位文件字段只算一个候选；本功能不自动应用启动参数。
6. 非 EXE 启动方式或不合法路径应提示不支持，不得将其直接追加 `.exe` 后填入。

## 5. 页面交互

建议在 AppID 输入框旁设置统一的「自动获取游戏信息」按钮，覆盖上述字段的获取需求，不再要求用户先填写 DepotID。

操作流程：

1. 用户填写有效的正整数 AppID，点击按钮。
2. 按钮显示「正在获取…」并禁止重复点击。
3. 获取并解析同一份应用数据，完成校验和候选筛选。
4. 唯一且有效的结果直接填入；存在歧义的字段通过候选选择完成。
5. 显示成功、部分成功或失败状态，具体说明未填入的字段及原因。
6. 用户可继续手动修改，再通过现有保存预设、生成配置流程使用这些值。

用户主动点击获取，意味着允许用本次有效结果更新对应字段，无需逐字段弹出确认。失败或缺失字段保留原值，但必须标明该值未被本次查询验证，避免误认为它属于当前 AppID。

获取期间若 AppID、当前预设或相关筛选条件发生变化，应取消或丢弃过期结果；用户在请求期间手动修改过的字段，不应被迟到的响应覆盖。候选选择结束后也应再次核对请求上下文。

自动获取只填充表单，不自动生成 ACF、不创建占位文件，也不直接调用保存。预设继续沿用现有保存按钮和离开页面时的自动保存行为。

## 6. 校验与异常处理

- AppID 按有效的正整数范围校验，拒绝空值、负数和非数字输入。
- 安装目录和占位 EXE 路径在填入及后续生成时执行相应校验，阻止绝对路径、目录穿越和非法路径。
- Manifest ID 使用字符串保存，避免大整数精度损失。
- Depot 选择改变时，Manifest 必须随之更新；无法获取对应值时明确提示，避免继续误用其他 Depot 的 Manifest。
- 对超时、连接失败、应用不存在、响应格式异常、字段缺失和无可用候选分别给出可理解的提示。
- 保留手动填写及 SteamDB 查询入口，不因自动获取失败阻断手动操作。
- 如果换了 AppID 后仍残留旧值，页面应提示其需要检查；不得把新旧数据混合后报告为全部获取成功。

## 7. 第一版范围与后续扩展

### 第一版建议纳入

- 仅凭 AppID 获取全部目标字段。
- 默认查询公开分支 `public`。
- 单 Depot、单 EXE 场景直接填充。
- 多候选展示与选择、部分成功处理、过期请求保护。
- 复用现有预设中的相关字段，保留手动编辑。
- 将现有误标为「从 SteamDB 自动获取」的相关文案改为准确的「自动获取」描述。

### 第一版不包含

- 多 Depot 同时写入 ACF 的完整支持。
- 私有／密码保护分支的查询与选择。
- 通过网络判断真实游戏 EXE 或启动器的本机安装位置。
- 自动下载游戏、自动安装或自动套用启动参数。

当前 ACF 生成逻辑仅写入一个 Depot。后续若要普遍支持需要多个 Depot 的游戏，需要将单个 DepotID／Manifest 扩展为 Depot 列表，并同步调整页面、预设结构、旧数据兼容和 ACF 生成逻辑。

## 8. 现有项目改动位置

| 位置 | 预计改动 |
| --- | --- |
| `Services/SteamService.cs` | 提取或复用应用信息请求，增加字段解析和候选获取，保留既有调用兼容 |
| `Views/Pages/CustomManifestPage.xaml` | 添加统一获取按钮、状态提示和候选选择界面 |
| `Views/Pages/CustomManifestPage.xaml.cs`、`CustomManifestPage.GameInfo.cs` | 页面生命周期接入、填充、选择联动、校验和过期结果保护 |
| `Models/AppSettings.cs` | 第一版目标字段已有存储位置；多 Depot 扩展时才需要调整持久化结构 |

按现有单 Depot 数据结构实现第一版，预计属于中小规模改动。完整多 Depot 支持应单独评估。

实现时新增 `Models/SteamAppInfo.cs` 保存完整的 Depot、启动项和分支数据；`SteamAppInfoService` 负责 HTTP 获取并支持注入 HttpClient 和接口地址，`SteamAppInfoParser` 负责解析，`SteamAppSelectionService` 通过选项控制平台、架构、语言及分支筛选，`SteamPathValidator` 统一路径校验。当前页面使用公开分支，底层无需按游戏写死规则即可支持后续分支选择。

名称采用页面所选游戏语言，并回退到通用名称。64 位 Windows 同时保留 32 位启动器候选。多候选选择使用页面内下拉框；每次查询开始清除上一轮候选，查询失败时保留表单值并标明未验证。

## 9. 验收标准

| 场景 | 预期结果 |
| --- | --- |
| 输入 `4162040` 后获取 | 填入名称、目录、`4162041`、`HYP.exe` 及接口当前返回的公开分支 BuildID／Manifest |
| 本地化名称缺失 | 使用通用名称；安装目录仍取 `installdir` |
| EXE 位于子目录 | 完整保留相对路径，后续占位生成仍使用该路径 |
| 多启动项指向同一 EXE、参数不同 | 去重后直接填入唯一 EXE 路径 |
| 多个不同 Windows EXE | 展示候选，按用户选择填入 |
| 存在明确不适用的平台候选 | 不自动选中这些候选 |
| 多个 Depot | 展示候选与单 Depot 限制，不随机选择，不承诺单项即可满足全部安装需求 |
| 更换所选 Depot | Manifest 与对应 Depot 保持一致 |
| 无 public Manifest 或缺少启动配置 | 明确说明缺失字段，不伪造结果，不报告全部成功 |
| 网络失败或 AppID 不存在 | 保留原有输入并提示失败，可继续手动填写 |
| 请求期间切换 AppID／预设或修改输入 | 过期结果不覆盖当前表单或用户新输入 |
| 获取成功 | 仅更新表单；保存和生成仍由用户通过原有操作触发 |

## 10. 参考资料

- [SteamCMD API 文档](https://www.steamcmd.net/)：接口形式、响应示例和认证说明。
- [Steamworks Depot 文档](https://partner.steamgames.com/doc/store/application/depots)：Depot 的用途和配置。
- [绝区零 SteamDB 配置页](https://steamdb.info/app/4162040/config/)：启动文件与安装目录的手动核对入口。
- [SteamDB FAQ](https://steamdb.info/faq/)：自动抓取限制说明。

附件截图仅作为字段示例；本文需求来源于用户的对话请求。
